name: pemrograman_mobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  # 📦 Library utama
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.4
  open_file: ^3.3.2
  csv: ^6.0.0
  pdf: ^3.10.8
  fl_chart: ^0.68.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Jika nanti ingin menambahkan gambar atau file lain:
  # assets:
  #   - assets/images/
